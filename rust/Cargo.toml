[package]
name = "agnosgram"
version = "1.6.0"
edition = "2021"
description = "agent-agnostic, per-project memory (plain Markdown in your repo)"
license = "MIT"
repository = "https://github.com/brolyssjl/agnosgram"
keywords = ["ai-agents", "memory", "markdown", "cli"]
categories = ["command-line-utilities"]
publish = false

[[bin]]
name = "agnosgram"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = 3
