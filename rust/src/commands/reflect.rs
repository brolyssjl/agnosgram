//! Port of `src/commands/reflect.ts`. Wave 1 stub - wave 2 fills this in.

use crate::core::output::UserError;

pub fn run(_argv: Vec<String>) -> Result<(), UserError> {
    Err(UserError::new("not yet ported"))
}
