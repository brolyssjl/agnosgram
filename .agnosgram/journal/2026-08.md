# Journal - 2026-08

Append-only. One file per month. Four fixed slots per entry so distillation is
mechanical: `Learned` lines are lesson candidates, `Decided` lines are ADR
candidates.

<!-- Entry format (newest at the bottom; `agnosgram log` appends here):

## 2026-08-DD HH:MM · <agent> · <branch>
- **Did:** ...
- **Learned:** ...
- **Decided:** ...
- **Avoid:** ...
- **Next:** ...

-->

## 2026-08-02 22:53 · claude · milestone-6-surface-freeze
- **Did:** Milestone 6 Round 1: shared parseCliArgs wrapper (src/core/args.ts) routing all 11 commands, fixing FRI-001 (dash-leading option values no longer crash parseArgs); symlink-aware adapt (FRI-003, CLAUDE.md<->AGENTS.md collapse to one honest write); introduced conformance mode - src/conformance/harness.ts spawns AGNOSGRAM_BIN, npm run conformance runs the *.conformance.test.ts subset, migrated the full CLI-surface test suite off in-process runX calls; version bumped to 0.11.0
- **Learned:** the dash-leading-value crash generalizes beyond numeric options - any string option, and even unknown-option/unexpected-positional cases, previously raw-crashed since cli.ts only caught UserError; the fix is a pre-parseArgs token rewrite (only when the next token isn't itself a recognized flag) plus a catch-all ERR_PARSE_ARGS_* to UserError conversion
- **Decided:** conformance vs unit split by whether the test exercises argv parsing/stdout/exit codes (spawn the binary) vs pure internal functions (stay in-process); doctor.test.ts and core/records.test.ts needed no migration since they already only unit-tested collectFindings/loadRecords, never runDoctor's CLI surface - added a new doctor.conformance.test.ts to cover that surface for the first time
- **Next:** coordinator review of milestone-6-surface-freeze; conformance acceptance run recorded 14 expected diffs (11 FRI-001 dash tests + 3 FRI-003 symlink tests) against the installed 0.10.0 binary, zero unexpected diffs
