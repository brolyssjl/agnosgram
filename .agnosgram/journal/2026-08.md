# Journal - 2026-08

Append-only. One file per month. Four fixed slots per entry so distillation is
mechanical: `Learned` lines are lesson candidates, `Decided` lines are ADR
candidates.

<!-- Entry format (newest at the bottom; `agnosgram log` appends here):

## 2026-08-DD HH:MM · <agent> · <branch>
- **Did:** ...
- **Learned:** ...
- **Decided:** ...
- **Avoid:** ...
- **Next:** ...

-->

## 2026-08-02 22:53 · claude · milestone-6-surface-freeze
- **Did:** Milestone 6 Round 1: shared parseCliArgs wrapper (src/core/args.ts) routing all 11 commands, fixing FRI-001 (dash-leading option values no longer crash parseArgs); symlink-aware adapt (FRI-003, CLAUDE.md<->AGENTS.md collapse to one honest write); introduced conformance mode - src/conformance/harness.ts spawns AGNOSGRAM_BIN, npm run conformance runs the *.conformance.test.ts subset, migrated the full CLI-surface test suite off in-process runX calls; version bumped to 0.11.0
- **Learned:** the dash-leading-value crash generalizes beyond numeric options - any string option, and even unknown-option/unexpected-positional cases, previously raw-crashed since cli.ts only caught UserError; the fix is a pre-parseArgs token rewrite (only when the next token isn't itself a recognized flag) plus a catch-all ERR_PARSE_ARGS_* to UserError conversion
- **Decided:** conformance vs unit split by whether the test exercises argv parsing/stdout/exit codes (spawn the binary) vs pure internal functions (stay in-process); doctor.test.ts and core/records.test.ts needed no migration since they already only unit-tested collectFindings/loadRecords, never runDoctor's CLI surface - added a new doctor.conformance.test.ts to cover that surface for the first time
- **Next:** coordinator review of milestone-6-surface-freeze; conformance acceptance run recorded 14 expected diffs (11 FRI-001 dash tests + 3 FRI-003 symlink tests) against the installed 0.10.0 binary, zero unexpected diffs

## 2026-08-19 22:39 · claude · milestone-6-round1-closeout
- **Did:** Milestone 6 Round 1 closeout: wrote DEC-0004 declaring the CLI surface freeze as the Rust port contract, ticked all four Round 1 checkboxes in ROADMAP.md, and rewrote .agnosgram/state/status.md (stale since 2026-07-30, still claiming Milestone 5 in-flight after both M5 and M6 Round 1 had merged)
- **Learned:** status.md staleness compounds silently - it survived one full milestone (M5 merge) and one full round (M6 Round 1 merge, PR #8) without being touched, because updating it wasn't part of either merge
- **Decided:** DEC-0004: the CLI surface (11 commands, flags, --json shapes, exit codes) as exercised by the conformance suite is frozen as the Milestone 6 port contract; DEC-0002's on-disk format contract is unchanged and non-negotiable
- **Avoid:** don't treat status.md as a someday chore - update it in the same PR that merges the milestone/round it describes, not in a separate closeout pass
- **Next:** fix FRI-002 (GitHub Release binary assets for tagged versions + docs for read-only npm prefixes), then start Milestone 6 Round 2 (the Rust port)

## 2026-08-19 23:05 · agent · fri-002-install-story
- **Did:** Resolved FRI-002 (install story) on fri-002-install-story: README.md and docs/install.md now lead with install.sh/prebuilt binaries (shipped since 0.9.0: linux-x64, darwin-arm64) as the primary install path, with a read-only-global-npm-prefix (nix, managed machines) troubleshooting subsection covering the two working options; install.sh's fallback for unsupported platforms/missing assets now prints honest build-from-source steps (git clone, npm ci, npm run build, node dist/cli.js or npm link) instead of a guaranteed-404 npm install -g agnosgram, matching the Milestone 6 npm-leaves-the-install-path decision
- **Learned:** The FRI-001/FRI-003 resolution convention (commit 2a7f68f) is: keep the original friction paragraph verbatim, prepend a 'RESOLVED (version): ...' note above it inside the same record body, and bump only last_verified - the id/created/scope/confidence fields stay untouched
- **Decided:** install.sh's npm fallback is retired entirely in favor of build-from-source instructions, since the package is not on npm yet and an npm install fallback can only 404; the header comment's 'keep in sync with release.yml' platform-list contract stays intact
- **Avoid:** Do not leave docs/install.md and README.md's install sections out of sync - docs/install.md is the detailed page README links to, so a docs-only fix to one without the other reintroduces the same friction
- **Next:** Milestone 6 Round 2: the Rust port

## 2026-08-19 23:10 · claude · fri-002-install-story
- **Did:** Review pass on fri-002-install-story: E2E install test 404'd against a release with assets, root cause = private repo (unauthenticated asset downloads and the raw install.sh one-liner always 404); added a gh release download fallback to install.sh (curl stays primary), private-repo notes to README/docs/install.md, and extended the FRI-002 resolution note
- **Learned:** gh release view showing assets proves nothing about the anonymous download path - private repos 404 unauthenticated curl even for existing assets, so install stories must be verified end-to-end as an outsider would run them
- **Decided:** curl-first with gh fallback, so install.sh needs no change when the repo goes public
- **Avoid:** don't declare an install path fixed from repo-side evidence alone; run install.sh against the live release before closing install friction
- **Next:** Milestone 6 Round 2: the Rust port

## 2026-08-20 20:59 · claude · milestone-6-rust-port
- **Did:** Milestone 6 Round 2: ported the full CLI to a zero-dependency Rust crate (rust/, 1:1 file map per docs/rust-port.md) in coordinated waves - core modules, then init/adapt/adapters/hooks, then the remaining 9 commands in two parallel groups, then CI/release wiring; full conformance suite 104/104 against the Rust binary, 202 Rust unit tests, TS suite untouched at 307
- **Learned:** byte-exactness lives in the corners: Node parseArgs error first-lines, JSON.stringify key order and escaping, UTF-16 slice semantics (U+FFFD on split surrogates), backslash-newline continuations silently eating template indents, and a one-vs-many whitespace regex mismatch that hid a record from distill --validate
- **Decided:** std-only crate (no external crates): hand-rolled JSON emitter/parser matching JSON.stringify(v,null,2), localtime via a direct localtime_r FFI; conformance suite stays the normative surface definition and release.yml now hard-gates on it plus a tag/package.json/Cargo.toml version guard
- **Avoid:** do not accept a subagent's green conformance run as proof of byte-equality - the suite mostly asserts includes(), so twin-dir byte-diffs against the TS binary are what actually caught the template-indent and truncation divergences
- **Next:** watch the PR's ubuntu CI leg (first linux run of the Rust binary), tick Round 2 checkboxes when green, merge, then Round 3 (rc soak, upgrade story, 1.0.0)

## 2026-08-21 23:23 · claude · main
- **Did:** Released 1.0.0 directly from the merged Round 2 Rust port (owner decision, no rc cycle): bumped package.json + rust/Cargo.toml to 1.0.0, switched main.rs VERSION to env!(CARGO_PKG_VERSION) so Cargo.toml is the single Rust-side version source, reframed ROADMAP Round 3 as post-1.0.0 hardening, tagged v1.0.0
- **Learned:** the hardcoded VERSION constant in main.rs was a second bump site waiting to be forgotten; compiling it from CARGO_PKG_VERSION plus the release workflow's tag/package.json/Cargo.toml guard leaves exactly one manual bump per file family
- **Decided:** 1.0.0 ships without an rc cycle: the cross-implementation conformance suite green on both release platforms plus the release pipeline's hard conformance gate are the acceptance evidence; remaining Round 3 items become post-1.0.0 hardening
- **Avoid:** never tag before re-running the full matrix locally (cargo test, conformance vs the rebuilt binary, npm test) - the version guard only catches metadata drift, not behavior
- **Next:** watch the v1.0.0 release workflow (guard, conformance gate, cargo binaries); then gate's Rust port

## 2026-08-22 14:31 · claude · readme-one-oh
- **Did:** Updated README.md and docs/install.md for 1.0.0: Rust binary is canonical distribution (DEC-0004), removed every npm-as-install mention (npm section, read-only-prefix subsection, npx/npm-i-g examples) from both docs plus install.sh's build-from-source fallback and docs/claude-hooks.md; build-from-source is now cargo build --release --manifest-path rust/Cargo.toml; kept the private-repo/gh-fallback caveat; added 1.0.0 to README release history; aligned CONTRIBUTING.md's npm-publish section with the permanent no-npm decision
- **Learned:** FRI-002's resolution text and DEC-0004 made this a clean removal, not a redesign: the private-repo caveat and gh fallback in install.sh/docs were already correct and needed no changes, only the npm-as-a-future-option framing had to go
- **Decided:** Treated CONTRIBUTING.md's dormant publish-npm workflow job as out of scope to remove (workflows are off-limits per task scope) but reworded its docs to say the owner decision keeps it permanently disabled rather than 'enable when ready'
- **Avoid:** Do not leave 'once published' or 'until that changes' phrasing around npm - the decision is permanent, not pending
- **Next:** Round 3 post-1.0.0 hardening: upgrade story (version-stamped managed blocks, doctor staleness warnings, adapt --refresh diff), daily-driver soak, docs site + case study

## 2026-08-22 18:05 · claude · audit-one-oh
- **Did:** Post-1.0.0 audit (docs + implementation): fixed CONTRIBUTING release steps (rust/Cargo.toml lockstep bump), struck the superseded npm rows/bullets in ROADMAP, corrected the 1.0.0 release-table row, removed orphaned build:binary tooling, removed wave-narrative comments from rust sources and workflows, dropped the crate-wide allow(dead_code) - deleted 4 genuine orphans (LintPattern + 3 args builder methods), cfg(test)-scoped 8 test-only items, item-level allows on 4 TS-fidelity fields
- **Learned:** the soul audit passed with byte-level proof (twin-dir diffs of init/log/feedback identical between TS and Rust binaries; zero Command::new in the crate) - the risks that survive a port are hygiene and doc drift, not behavior
- **Decided:** docs/rust-port.md stays as the port's design record with a status banner: its self-deletion condition (TS retired) has not happened - TS remains the normative reference per DEC-0004
- **Avoid:** never leave a crate-wide allow(dead_code) past the milestone that justified it; it hides real orphans behind fidelity ports
- **Next:** Round 3 hardening (upgrade story, daily soak, docs site); gate release tag decision

## 2026-08-23 12:05 · claude · pack-lint-and-pins
- **Did:** Owner-decisions round: pack injection lint (warn-and-mark, SEC-07), README/adapters.md automatic-memory scoping (PUR-02), architecture.md refresh, GitHub Actions SHA-pinning + Dependabot (SEC-08), ROADMAP bookkeeping
- **Learned:** pack's greedy budget simulation needs the same up-front worst-case reserve trick for a new fixed cost (the injection banner) as it already used for the omitted-records footer - compute whether the cost is needed before the first simulate_pack pass, not after
- **Decided:** pack warns and marks on injection hits rather than refusing - a false positive must not become a missing lesson
- **Next:** Round 3 hardening: upgrade story, soak on real host projects, reflect-cadence heartbeat (RM-04), go-public decision (RM-03) still owner-deferred
