# Journal - 2026-09

Append-only. One file per month. Four fixed slots per entry so distillation is
mechanical: `Learned` lines are lesson candidates, `Decided` lines are ADR
candidates.

<!-- Entry format (newest at the bottom; `agnosgram log` appends here):

## 2026-09-DD HH:MM · <agent> · <branch>
- **Did:** ...
- **Learned:** ...
- **Decided:** ...
- **Avoid:** ...
- **Next:** ...

-->

## 2026-09-02 22:20 · claude · distill-validate-polish
- **Did:** Implemented GitHub issues #26, #27, #28 as three sequential branches/PRs off main: subcommand-help (PR #29), freshness-source-of-truth (PR #30), distill-validate-polish (this branch). Full cargo fmt/clippy/test suite green on each.
- **Learned:** gate's FRI-004 fix centralizes -h/--help interception in one place before dispatch (main()), not per-subcommand arg loops; mirrored that shape here since agnosgram's 11 commands already route through one shared main.rs dispatch point rather than each owning its own top-level parse.
- **Decided:** distill --validate's new multi-file support is hand-parsed ahead of the shared core::args parser (which has no variadic-option support) rather than extending that shared module, to keep the change scoped to distill.rs alone.
- **Next:** PR3 (distill-validate-polish) branches from the same original main as PR1/PR2, not rebased on top of them, per instructions - its doctor --strict help-text line will need a trivial rebase once PR1's per-subcommand help refactor merges.

## 2026-09-05 20:12 · agent · chore/store-distill-freshness
- **Did:** Released 1.4.0 (git.untracked doctor check); distilled journal 2026-08/09 into LES-003..005, CON-004..006, DEC-0005; archived absorbed months; refreshed status.md + Freshness table
- **Learned:** the new git.untracked check flagged this session's own uncommitted ADR within minutes of shipping - the check pays for itself immediately in real workflows
- **Next:** owner go-public decision (RM-03); keep reflect cadence per soak

## 2026-09-05 21:09 · agent · feat/trust-posture-38-39
- **Did:** Implemented issues #38/#39: shared untrusted-content helpers in core::lint, warn-and-mark banners + standing trust note in reflect/distill/advise prompts, owner-only dogfooding posture in README + docs/feedback.md, hostile-fixture conformance tests
- **Learned:** pack's banner wording and budget-reserve logic are frozen surface, so the shared helpers mirror its shape instead of refactoring it
- **Next:** merge PR #40 (roadmap) then #41; then the go-public decision

## 2026-09-05 22:22 · agent · fix/lint-cross-line-injection-43
- **Did:** Fixed agnosgram#43: core::lint::scan_patterns is strictly per-line, so injection phrasing straddling an ordinary Markdown hard-wrap evaded every matcher
- **Learned:** Added a paragraph-normalized second pass (scan_injections_with_paragraphs) that collapses whitespace across a blank-line-delimited paragraph and attributes hits to the paragraph's first line, deduped against per-line hits already found in that paragraph's line range; routed pack's scan_injections, doctor's injection lint, and scan_untrusted (reflect/distill/advise) through it, while doctor's secret scan stays strictly per-line since secrets are single-line artifacts
- **Next:** PR open against main referencing Closes #43, not yet merged

## 2026-09-22 20:14 · claude · chore/post-publish-housekeeping
- **Did:** Owner merged PR #47 (elephant mascot + README polish; sister gate#51 merged the same day). Post-publish housekeeping: checked the RM-03 go-public item in ROADMAP.md (public since 2026-09-15), split the still-open pilot-user half into its own item, refreshed state/status.md and the Freshness table
- **Learned:** The go-public decision left the roadmaps and status.md stale for a week because the flip was recorded only in the session memory, not in the store - closing an owner-decision item needs a store edit in the same session as the decision
- **Next:** Upgrade story (version-stamped managed blocks, doctor stale-artifact warnings, adapt --refresh diff) is the next scheduled build item; gate's doctor/update is the template. gate repo needs the matching journal entry and roadmap fix (its checkout was left untouched this session)

## 2026-09-22 22:42 · agent
- **Did:** Rewrote README.md to be short and human-oriented (256 -> 142 lines); moved Trust posture + Feedback-from-users into docs/trust-posture.md (folded feedback bit into existing docs/feedback.md 'Who this is for' heading), How it compares + Design principles into new docs/how-it-compares.md, and Anti-rot into docs/doctor.md; dropped the Status & roadmap section entirely since ROADMAP.md already holds that content
- **Learned:** docs/feedback.md's 'Who this is for' paragraph was bold text, not a real heading, so it had no anchor to link from trust-posture.md - promoted it to a ## heading rather than leaving a dead cross-reference
- **Next:** consider trimming docs/how-it-compares.md's arrow characters/wording further if a future pass wants tighter prose; no other README-related follow-up known

## 2026-09-22 23:13 · agent
- **Learned:** Managed-block recognition (finding 3, 2026-09-22 audit): find_blocks now requires the start marker to begin a line and be followed by the sentinel line agnosgram writes, and requires balanced markers, else upsert_managed_block refuses with a UserError naming the file/line instead of silently deleting content between a stray marker and the real block.

## 2026-09-22 23:15 · claude
- **Did:** Fixed doctor.rs check_broken_links and advise.rs resolve_plan_file (2026-09-22 audit findings 7 and 8): both let an attacker-controlled path (absolute or ../ escape) turn a diagnostic into a read/existence oracle over arbitrary files
- **Learned:** A read-side containment check needs the same rigor as a write-side one: lexically normalize before any filesystem probe, and canonicalize both sides only after existence is confirmed, so classification never depends on what exists on disk
- **Next:** none - task read-containment is done, PR open

## 2026-09-22 23:21 · claude
- **Did:** Fixed a residual existence oracle in doctor.rs check_broken_links found on PR 51 review: a committed symlink inside the store lexically normalized under root, so path_exists still followed it at probe time
- **Learned:** Lexical containment is not enough once a symlink can sit inside the allowed tree - need a separate fs::symlink_metadata walk of the path prefixes, and must not canonicalize for that decision since success/failure of canonicalize on a dangling symlink is itself an oracle
- **Next:** none - fix pushed to fix/read-containment, PR 51 updated

## 2026-09-22 23:19 · claude
- **Did:** Fixed agnosgram security audit findings 1 and 2 (2026-09-22): added project-root containment + atomic writes to write_if_changed, containment-aware symlink alias grouping and pre-read check in apply_adapter, and content-signature gating for adapt --refresh's hook reinstall.
- **Learned:** A committed repo symlink (adapter target, or .claude/settings.json / .claude directory) plus path-existence-only checks is enough to redirect agnosgram writes outside the project, including into the user's global Claude Code config.
- **Decided:** write_if_changed now canonicalizes the deepest existing ancestor and refuses non-regular-file targets before any read or write; writes are atomic (temp file + rename). apply_adapter validates containment before reading. group_by_symlink only collapses a CLAUDE.md<->AGENTS.md-style alias when the real path stays inside root. hooks_installed checks for a stable content signature, not just path existence.
- **Avoid:** Never treat 'a file exists at this path' as proof of anything about its provenance or safety when that path can be repo-controlled.
- **Next:** None planned for this task; findings 3-8 from the same audit are owned by other tasks.
