# Journal — 2026-07

Append-only. One file per month. Four fixed slots per entry so distillation is
mechanical: `Learned` lines are lesson candidates, `Decided` lines are ADR
candidates.

<!-- Entry format (newest at the bottom; `agnosgram log` appends here):

## 2026-07-DD HH:MM · <agent> · <branch>
- **Did:** ...
- **Learned:** ...
- **Decided:** ...
- **Avoid:** ...
- **Next:** ...

-->

## 2026-07-21 23:30 · claude-code · main
- **Did:** Built Milestone 1: init, adapt, log, MEMORY protocol, templates
- **Learned:** node:util parseArgs + node:test give a zero-dependency CLI and test setup
- **Decided:** Minimal hand-rolled YAML subset over a dependency
- **Next:** Milestone 2: distill, doctor, advise

## 2026-07-23 22:39 · claude-code · milestone-2-anti-rot
- **Did:** Built Milestone 2: frontmatter schema+validator, doctor (schema/staleness/budgets/links/ids/near-dup + secret & injection lints), distill and bootstrap prompt commands, and the format freeze (v1)
- **Learned:** Existing frontmatter uses inline flow sequences (scope: [a, b]) which the hand-rolled YAML did not parse; widening parseScalar to read flow sequences was the clean fix, staying dependency-free per DEC-0001
- **Decided:** Froze the .agnosgram/ layout + frontmatter schema as format version 1 (DEC-0002), additive-only; doctor is the executable spec of that contract
- **Avoid:** Do not import a real tokenizer at runtime for budgets; the zero-runtime-dep guarantee wins, so tokens.ts uses a heuristic estimate
- **Next:** Milestone 3: advise (contradiction-catcher), pack, show, pluggable JSON/TOON serializer
