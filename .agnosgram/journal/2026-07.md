# Journal — 2026-07

Append-only. One file per month. Four fixed slots per entry so distillation is
mechanical: `Learned` lines are lesson candidates, `Decided` lines are ADR
candidates.

<!-- Entry format (newest at the bottom; `agnosgram log` appends here):

## 2026-07-DD HH:MM · <agent> · <branch>
- **Did:** ...
- **Learned:** ...
- **Decided:** ...
- **Avoid:** ...
- **Next:** ...

-->

## 2026-07-21 23:30 · claude-code · main
- **Did:** Built Milestone 1: init, adapt, log, MEMORY protocol, templates
- **Learned:** node:util parseArgs + node:test give a zero-dependency CLI and test setup
- **Decided:** Minimal hand-rolled YAML subset over a dependency
- **Next:** Milestone 2: distill, doctor, advise

## 2026-07-23 22:39 · claude-code · milestone-2-anti-rot
- **Did:** Built Milestone 2: frontmatter schema+validator, doctor (schema/staleness/budgets/links/ids/near-dup + secret & injection lints), distill and bootstrap prompt commands, and the format freeze (v1)
- **Learned:** Existing frontmatter uses inline flow sequences (scope: [a, b]) which the hand-rolled YAML did not parse; widening parseScalar to read flow sequences was the clean fix, staying dependency-free per DEC-0001
- **Decided:** Froze the .agnosgram/ layout + frontmatter schema as format version 1 (DEC-0002), additive-only; doctor is the executable spec of that contract
- **Avoid:** Do not import a real tokenizer at runtime for budgets; the zero-runtime-dep guarantee wins, so tokens.ts uses a heuristic estimate
- **Next:** Milestone 3: advise (contradiction-catcher), pack, show, pluggable JSON/TOON serializer

## 2026-07-26 22:31 · agent · milestone-2-anti-rot
- **Did:** Fixed 8 review findings on the milestone-2 branch: archive-aware source resolution in doctor, YAML-shaped fence detection (thematic breaks in bodies no longer derail records), strict YAML subset (block scalars and leftover lines now throw), bodies read from unmasked text, config version guard, portable path comparison in distill --validate, apostrophe-safe comment stripping; docs updated, +9 regression tests (72 passing)
- **Learned:** Positional --- fence pairing is unsound for Markdown bodies; a fence only opens a record when every line up to the closing fence is YAML-shaped. Silent-drop YAML parsing let distill --validate bless mis-parsed frontmatter - the subset must fail loudly
- **Avoid:** Do not freeze a format while its parser accepts undefined input silently; doctor-as-executable-spec only works if the parser rejects what the spec does not define
- **Next:** Update PR #2 with the fixes and the review findings

## 2026-07-26 22:59 · agent · main
- **Did:** Merged PR #2 (Milestone 2 + review fixes) and released v0.5.0 on GitHub via the tag-driven workflow; npm publish still deferred
- **Next:** Milestone 3: advise, pack, show, pluggable JSON/TOON serializer

## 2026-07-27 10:03 · claude · milestone-3-retrieval
- **Did:** Implemented Milestone 3 (Retrieval): pluggable JSON/TOON serializer, show, pack, advise, faithful TOON bench encoder, Tier-2 evals; bumped to 0.8.0
- **Learned:** The internal estimateTokens heuristic under-counts Markdown-heavy pack output by roughly 35-40% versus real GPT tokenization (measured via Tier-2 bench on a 40-record fixture store); pack's budget enforcement is honest about being a proxy (DEC-0001) but the gap is worth watching
- **Decided:** pack_budget is an additive, optional config.yml key (no store schema change, CON-003 discipline); the advise report schema is a versioned CLI output contract (agnosgram_advise: 1), not part of the frozen .agnosgram/ store format
- **Next:** Push milestone-3-retrieval to origin for review; open the M3 PR after review; then start Milestone 4

## 2026-07-29 19:50 · claude · milestone-4-reach
- **Did:** Implemented Milestone 4 (Reach) on branch milestone-4-reach: Windsurf/Cline/Roo adapters, Codex/OpenCode routed to the AGENTS.md adapter, path-aware SDD coexistence hints, opt-in Claude Code hooks + skill (adapt --claude-hooks), install.sh + single-file binary (bun/Node SEA, verified end-to-end on init/pack/doctor), and docs (adapters.md, claude-hooks.md, install.md).
- **Learned:** Node SEA cannot load a multi-file ESM dist/ directly (it embeds one file); bundle to CJS with esbuild first. Also: copying a read-only source node binary (e.g. a Nix store path) can carry read-only mode to the copy, and postject needs the copy writable; and macOS blocks postject on a signed binary, so strip and re-apply an ad hoc signature around the injection step.
- **Decided:** Hook scripts under .claude/hooks/ are Node (.mjs), not bash, so JSON construction uses JSON.stringify instead of hand-escaped shell strings; settings.json merge identifies our own hook entries by script path in the command string and only ever updates or appends that entry, never touching unrelated hooks.
- **Next:** Coordinator review of milestone-4-reach, then open the Milestone 4 PR; after merge, begin the 1.0.0 real-world soak (dogfood 0.9.0, then docs site + case study).
